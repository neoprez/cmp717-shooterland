package game.code;


public class Pyro extends Character {
    private static final String NAME = "pyro.png";
    public Pyro(int x, int y, int startAngle){
        super(x, y, NAME, startAngle);
    }
}
